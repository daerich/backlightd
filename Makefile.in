PROG=backlightd
SRCS= backlightd.c
SRCS1= backlightctl.c
PROG1= backlightctl
# Your compiler
CC=gcc
OPTIONS="-Wall"
VERSION='"1.8"'
# Configure to your liking
DAEMONGROUP='"video"'
CONFIGMACRO='"/home/daerich/backlightd/backlight.conf"'
CONFIG="/home/daerich/backlightd/backlight.conf"

all: backlightctl backlightd

backlightctl:
	${CC} ${OPTIONS} -std=gnu17 ${SRCS1} -DVERSS=${VERSION} -DCONFIGS=${CONFIGMACRO} -o ${PROG1}

backlightd:
	${CC} ${OPTIONS} -std=gnu17 ${SRCS} -DVIDEOGROUPT=${DAEMONGROUP} -DCONFIGS=${CONFIMACRO} -o ${PROG}

dist: all install

test:
	${CC} ${OPTIONS} -g -std=gnu17 -DDEBUG -DVIDEOGROUPT=${DAEMONGROUP} -DDPATH='"/tmp/backlight_test/"'  ${SRCS} -o ${PROG}
	${CC} ${OPTIONS} -g -std=gnu17 -DDEBUG ${SRCS1} -DVERSS=${VERSION} -DCONFIGS='"/tmp/backlight_test/backlight_d.conf"' -o ${PROG1}

install:
	install -v -D -m 755 ${PROG1} /opt/bin/${PROG1}
	install -v -D -m 644 distrib/backlight.conf ${CONFIG}

clean:
	rm -v ${PROG} ${PROG2}
